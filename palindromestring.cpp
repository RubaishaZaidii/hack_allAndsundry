#include<iostream>
using namespace std;

bool isPalindrome(string *s,string *x)
{
        
}
int main()
{
    string *s="abba",*x;
    for (int i = 0; i < s.length(); i++)
    {
        x+=s[i];
    }
    cout<<"it is palindrome"<<isPalindrome(s,x);
    return 0;
}